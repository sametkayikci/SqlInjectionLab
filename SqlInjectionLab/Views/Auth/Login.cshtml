@{
    ViewData["Title"] = "Login";
}

<h2>Login</h2>

<form method="post" class="card p-3 col-6">
    <label>Kullanıcı Adı</label>
    <input name="username" class="form-control" />

    <label class="mt-2">Şifre</label>
    <input name="password" type="password" class="form-control" />

    <button class="btn btn-primary mt-3">Giriş Yap</button>

    @if (ViewBag.Error != null)
    {
        <p class="text-danger mt-2">@ViewBag.Error</p>
    }
</form>

<hr />

<h4>SQL Injection Payload Önerileri</h4>
<ul>
    <li><code>' OR '1'='1</code></li>
    <li><code>'--</code></li>
    <li><code>'; DROP TABLE users; --</code></li>
</ul>